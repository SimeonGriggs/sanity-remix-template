// TODO: IWBN if this could generate a new projectId + dataset
const main = () => {
  console.log(`
    Last step: Duplicate ".env.template" to ".env"
    Then visit sanity.io/manage to retrieve your projectId and dataset
  `)
}

module.exports = main
