// TODO: IWBN is this could generate a new projectId + dataset
const main = () => {
  console.log(`Last step: Duplicate ".env.template" to ".env"`)
  console.log(`Then visit sanity.io/manage to retrieve your projectId and dataset`)
}

module.exports = main
